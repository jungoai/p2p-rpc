name:           "n1"
httpEndpoint:   "http://127.0.0.1:5001" # NOTE: you should configure a reverse proxy
                                        # to map @httpEndpoint/<chain-id> to
                                        # the running chain address
httpPort:       4001
p2pPort:        3101
isBootstrap:    true # (default: false)
localTest:      true # (default: false)
bootstrappers:  []
